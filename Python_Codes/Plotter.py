# from Functions import plotter
#
# maximum_segment_size_list = [i * 100 for i in range(1, 11)]
# plotter(maximum_segment_size_list, "TXTs/results.txt")
#
# segment_loss_probability_list = [i / 100 for i in range(1, 21)]
# plotter(segment_loss_probability_list, "TXTs/results.txt")
